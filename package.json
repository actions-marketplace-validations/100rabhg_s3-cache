{
  "name": "s3-cache",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "ncc build index.js -o dist/main && ncc build post.js -o dist/post"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0"
  }
}